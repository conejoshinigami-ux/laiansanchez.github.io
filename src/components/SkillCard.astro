---
const { name, level } = Astro.props;
---

<div class="bg-white p-6 rounded-2xl shadow hover:shadow-lg transition">
  <h3 class="text-xl font-semibold">{name}</h3>
  <p class="text-teal-600">{level}</p>
</div>
