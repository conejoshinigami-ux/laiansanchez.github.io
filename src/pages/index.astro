---
import "../styles/global.css";
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
---

<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Inicio | LuisDev</title>

    <style>
      @keyframes fadeUp {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .fade-up {
        opacity: 0;
        animation: fadeUp 1s ease-out forwards;
      }

      /* Fondo con degradado sutil */
      body {
        background: radial-gradient(
          circle at top center,
          #0f111a 0%,
          #08090e 100%
        );
        color: #e5e7eb;
      }

      /* Header fijo */
      header.hero-fixed {
        position: sticky;
        top: 0;
        z-index: 20;
        background: rgba(10, 10, 15, 0.8);
        backdrop-filter: blur(10px);
        border-bottom: 1px solid rgba(0, 217, 255, 0.1);
      }
    </style>
  </head>

  <body class="font-sans text-gray-200">
    <!-- HEADER FIJO CON NAVBAR Y PRESENTACIÓN -->
    <header class="hero-fixed">
      <Navbar />

      <div
        class="max-w-6xl mx-auto px-6 md:px-8 py-20 text-center fade-up"
      >
        <h1 class="text-5xl md:text-6xl font-extrabold text-white">
          Hola, soy
          <span class="text-[#00d9ff] drop-shadow-[0_0_12px_#00d9ff88]"
            >Luis</span
          >
          👋
        </h1>
       
      </div>
    </header>

    <!-- CONTENIDO PRINCIPAL -->
    <main class="max-w-6xl mx-auto px-6 md:px-8 pt-16 pb-24 space-y-28">
      <!-- SOBRE MÍ -->
      <section
        class="fade-up bg-[#121214] border border-[#1f1f1f] rounded-2xl p-10 shadow-[0_0_25px_rgba(0,217,255,0.08)]"
      >
        <div class="flex flex-col md:flex-row items-center gap-10">
          <div class="space-y-6 md:w-2/3">
            <h2 class="text-3xl font-bold text-[#00d9ff]">👨‍💻 Sobre mí</h2>
            <p class="text-gray-300 leading-relaxed">
              Soy un desarrollador que combina la creatividad del diseño con la
              precisión del código. Mi meta es crear productos que no solo se
              vean bien, sino que funcionen con excelencia y transmitan
              profesionalismo.
            </p>

            <ul class="text-gray-300 list-disc list-inside space-y-2">
              <li>
                <span class="text-[#00d9ff] font-semibold">Diseño intuitivo</span> — cada detalle importa.
              </li>
              <li>
                <span class="text-[#00d9ff] font-semibold">Código limpio</span> — estructura clara y mantenible.
              </li>
              <li>
                <span class="text-[#00d9ff] font-semibold">Rendimiento óptimo</span> — velocidad y fluidez en todas las plataformas.
              </li>
              <li>
                <span class="text-[#00d9ff] font-semibold">Colaboración</span> — trabajo en equipo para potenciar ideas.
              </li>
            </ul>

            <p class="text-gray-300">
              Siempre busco mejorar, aprender nuevas tecnologías y optimizar la
              experiencia de usuario con un enfoque moderno y eficiente.
            </p>
          </div>

          <!-- Imagen decorativa -->
          <div
            class="flex justify-center md:justify-end w-full md:w-auto"
          >
            <img
              src="https://cdn.dribbble.com/users/1162077/screenshots/3848914/programmer.gif"
              alt="Luis trabajando"
              class="w-52 md:w-60 rounded-full shadow-[0_0_30px_#00d9ff44] border border-[#00d9ff]/40 object-cover"
            />
          </div>
        </div>
      </section>

      <!-- FRASE FINAL -->
      <section class="text-center py-16 fade-up">
        <blockquote
          class="text-2xl italic text-gray-300 leading-relaxed max-w-3xl mx-auto mb-4"
        >
          “El diseño es la forma visual de la inteligencia. Crear es mi manera
          de conectar ideas con experiencias.”
        </blockquote>
        <p class="text-[#00d9ff] font-semibold text-lg">– Luis Dev</p>
      </section>
    </main>

    <Footer />
  </body>
</html>
