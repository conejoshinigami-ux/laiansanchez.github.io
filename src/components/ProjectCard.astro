---
const { title, description, img } = Astro.props;
---
<article class="card">
  <img src={img} alt={title} class="w-full h-40 object-cover rounded-lg mb-4" />
  <h3 class="font-semibold text-lg mb-2">{title}</h3>
  <p class="text-sm text-muted mb-3">{description}</p>
  <a href="#" class="btn inline-block text-center">Ver más</a>
</article>
