---
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
import "../styles/global.css";
---

<html lang="es">
  <head>
    <meta charset="utf-8" />
    <title>Blog | LuisDev</title>

    <style>
      @keyframes fadeUp {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .fade-up {
        opacity: 0;
        animation: fadeUp 1s ease-out forwards;
      }

      body {
        background: radial-gradient(
          circle at top center,
          #0f111a 0%,
          #08090e 100%
        );
        color: #e5e7eb;
      }

      header.hero-fixed {
        position: sticky;
        top: 0;
        z-index: 30;
        background: rgba(10, 10, 15, 0.8);
        backdrop-filter: blur(10px);
        border-bottom: 1px solid rgba(0, 217, 255, 0.1);
      }
    </style>
  </head>

  <body class="font-sans text-gray-200">

    <!-- HEADER FIJO -->
    <header class="hero-fixed">
      <Navbar />

      <div class="max-w-6xl mx-auto px-6 md:px-8 py-12 text-center fade-up">
        <h1 class="text-5xl md:text-6xl font-extrabold text-white">
          Blog &
          <span class="text-[#00d9ff] drop-shadow-[0_0_12px_#00d9ff88]">
            Artículos
          </span>
        </h1>
      </div>
    </header>

    <!-- CONTENIDO PRINCIPAL -->
    <main class="max-w-6xl mx-auto px-8 pt-20 pb-24 space-y-16 fade-up">

      <div class="grid md:grid-cols-3 gap-10">

        <!-- Post 1 -->
        <div class="bg-[#0f172a]/60 border border-white/10 p-6 rounded-xl hover:-translate-y-2 transition-all duration-300 hover:shadow-[0_0_20px_#00d9ff55]">
          <h3 class="text-xl font-semibold text-[#00d9ff] mb-3">Cómo mejorar tu portafolio web</h3>
          <p class="text-gray-400 text-sm leading-relaxed">
            Aprende los trucos de diseño y rendimiento que marcan la diferencia.
          </p>
        </div>

        <!-- Post 2 -->
        <div class="bg-[#0f172a]/60 border border-white/10 p-6 rounded-xl hover:-translate-y-2 transition-all duration-300 hover:shadow-[0_0_20px_#00d9ff55]">
          <h3 class="text-xl font-semibold text-[#00d9ff] mb-3">Astro vs React</h3>
          <p class="text-gray-400 text-sm leading-relaxed">
            Comparativa entre frameworks y casos de uso reales.
          </p>
        </div>

        <!-- Post 3 -->
        <div class="bg-[#0f172a]/60 border border-white/10 p-6 rounded-xl hover:-translate-y-2 transition-all duration-300 hover:shadow-[0_0_20px_#00d9ff55]">
          <h3 class="text-xl font-semibold text-[#00d9ff] mb-3">Diseño UX/UI moderno</h3>
          <p class="text-gray-400 text-sm leading-relaxed">
            Cómo lograr interfaces agradables y funcionales.
          </p>
        </div>

      </div>

    </main>

    <Footer />
  </body>
</html>
